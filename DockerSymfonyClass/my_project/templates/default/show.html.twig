{% extends 'base.html.twig' %}

{% block title %}Hello DefaultController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }
    </style>

    <div class="example-wrapper">
        <div >
            <h1>Hello {{ user.name }}! ✅</h1>
        </div>

        <ul>
            {% for video in videos %}
                <li>{{ video.id }} - {{ video.title }}</li>
            {% endfor %}
        </ul>

        <p>This guy live in {{ address.street }} on number {{ address.number }}</p>

    </div>

{% endblock %}
